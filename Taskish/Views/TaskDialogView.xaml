<UserControl x:Class="Taskish.Views.TaskDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:Taskish.Views"
             xmlns:vm="clr-namespace:Taskish.ViewModels">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:TaskDetailViewModel}">
            <views:TaskDetailView/>
        </DataTemplate>
    </UserControl.Resources>

    <Border
        Background="{StaticResource SurfaceContainerLowestBrush}"
        CornerRadius="20"
        Width="560"
        MinHeight="480"
        VerticalAlignment="Center"
        HorizontalAlignment="Center">

        <ContentControl Content="{Binding CurrentContent}"/>
    </Border>
</UserControl>
